function searchMap(item, p1, p2) {
	var posx, posy, posz, maxx, maxy, maxz, results = [], callback;
	maxx = p2.x;
	maxy = p2.y;
	maxz = p2.z;
	function searchMapForItem() {
		
	}
	for (posx = p1.x; posx < maxx; posx += 1) {
		for (posy = p1.y; posy < maxy; posy += 1) {
			// TODO
		}
	}
}
// Example
searchMap(5210, [31744, 30976, 0], [33536, 32768, 15]);
